imports:
- path shared-vpc-instance.jinja

resource:
- name: instance1
  type: instance.jinja
  properties:
    name: bastion
    zone: us-central1-c
  ##Update with host project ID
    hostProject: [host-project-ID]
    network: shared-vpc
    subnetwork: clusters
  ##Update staticIp with the bastion IP reserved in the host project. Use the IP address, not the name of the resource.
    staticIp: [xxx.xxx.xxx.xxx]
    networkTag: bastion
    #machineType:
    #sourceImage:
